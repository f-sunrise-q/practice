spring:
  application:
    name: consulClient1
  cloud:
      consul:
        host: localhost
        port: 8500
        enabled: true
        discovery:
          enabled: true
          prefer-ip-address: true
          hostname: 127.0.0.1
server:
  port: 8081

